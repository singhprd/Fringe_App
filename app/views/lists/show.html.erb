<p id="notice"><%= notice %></p>

<h2><%= @list.name %></h2>

<div class="row" data-controller="friend-requests">
  <div class="col-md-6">
    <% @list.list_items.map(&:event_id).each do |event_id| %>
    <% event = Event.find(event_id) %>
    <%= render :partial => "home/event_card", locals: { event: event } %>
    <% end %>
  </div>


  <div class="col-md-6">

    <div class="panel panel-primary home_card text-info" data-controller="friend-list">
      <div class='panel-heading'>
        <h3 class="panel-title">Add friends to list</h3>
      </div>

      <div class="container-fluid" id="home-card">
<!--           <div class="col-lg-6"> -->
    <div class="input-group">
      <input type="text" class="form-control" placeholder="Search for..." id="my-autocomplete-container">
      <span class="input-group-btn">
        <button class="btn btn-default" type="button">Add to list</button>
      </span>

<div id="the-basics">
  <input class="typeahead" type="text" placeholder="States of USA">
</div>

    </div><!-- /input-group -->
    </div><!-- /input-group -->
      </div>
    </div>
  </div>
</div>


  <div class=' col-xs-12'>
    <%= link_to 'Back', lists_path %> |
    <%= link_to 'Edit', edit_list_path(@list) %> |
    <%= link_to "Delete", list_path(@list), method: :delete, data: { confirm: 'Are you certain you want to delete this?' } %>
  </div>
