<% @events.each do |event| %>

<%= react_component('EventCard', event: event.to_json, venue: event.venue.to_json, user_signed_in: !current_user.nil?, is_favourited: Event.favourited?(current_user, event.id)) %>

<% end %>

<%= paginate @events %>
