<%= react_component('EventCardContainer',
  trace: nil,
  props: {
    eventJson: @events.map { |event| event.card_json(current_user) },
    currentUserId: current_user&.id
  }
)%>

<%= paginate @events -%>
