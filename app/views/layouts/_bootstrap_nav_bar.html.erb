<div style="height: 30px"></div> <!-- Dummy nav bar for blocking -->

<nav class="navbar navbar-default navbar-fixed-top" id="top-navbar">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <b><a class="navbar-brand" href="/">TheFringeRetrospective</a></b>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if params[:controller] == 'home' %>"><a href="/"><strong>Home</strong><span class="sr-only">(current)</span></a></li>
        <li class="<%= 'active' if params[:controller] == 'search' %>"><a href="/searches/new"><strong>Search</strong></a></li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Account<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="/users/edit">Account Options</a></li>
            <% if current_user.try(:admin?) %>
            <li><a href="/admin/panel">Admin Options</a></li>
            <% end %>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Sign out", "/users/sign_out", method: :delete, remote: true %>
            </li>
          </ul>
        </li>
        <% else %>      
        <li><a href="/users/sign_in">Sign in</a></li>
        <% end %>
      </ul>

      
 <!--      <form class="navbar-form navbar-right" action="/search/results" method="get">

        <div class="form-group">
          <input type="text" name="title_string" class="form-control" placeholder="Search">
        </div>

        <button type="submit" class="btn btn-default">Search</button>
      </form> -->


      <ul class="nav navbar-nav navbar-right">
        <p id="notice" class="navbar-text"><%= notice %></p>
        <p class="navbar-text"><%= alert %></p>
      </ul>

    </div>
  </div>
</nav>

