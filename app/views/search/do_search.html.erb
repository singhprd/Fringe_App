<%= link_to "<= back to search", :action => "index" %>

<% @events.each do |event| %>

<%= react_component('EventCard', event: event.to_json, venue: event.venue.to_json, user_signed_in: !current_user.nil?) %>

<% end %>
